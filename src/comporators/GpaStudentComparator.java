package comporators;

import java.util.Comparator;

import users.Student;

public class GpaStudentComparator implements Comparator<Student> {
    @Override
    public int compare(Student s1, Student s2) {
        // TODO: Implement comparison logic based on GPA
        return 0;
    }
}
